ListView.builder(
 itemCount: quiz.options.length,
 itemBuilder:(context,index){

 return OptionButton(
   text: quiz.options[index],
   onTap:(){
      checkAnswer(
        quiz.options[index]
      );
   },
 );

});
